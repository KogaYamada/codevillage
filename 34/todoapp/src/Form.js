import React from 'react';

const Form = () => {
  return (
    <form>
      <label>todoを入力</label>
      <input type="text" />
      <button type="button">登録</button>
    </form>
  );
};

export default Form;
